{
  let name = 'Douglas';
  let age = 24;
  let programmer = true;

  function hello(name: string) {
    console.log(`Hello ${name}`);
  }

  let arr = [1, 'douglas', true, null]; 

  const numbers = [1, 2, 3, 4, 5];
  const sum = numbers.reduce((acc, value) => acc + value, 0);
}