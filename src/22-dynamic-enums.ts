{
  enum Colors {
    BLUE = "#0000FF",
  }

  enum Colors {
    WHITE = 255,
    RED = 0xff0000,
    GREEN = 0x00ff00,
    TOTAL = 10 + 10,
    RESULT = "Hello".length,
  }

  const color: Colors = Colors.TOTAL;

  enum A {
    a,
    b,
    c,
  }

  enum A {
    d = 3,
  }
}
